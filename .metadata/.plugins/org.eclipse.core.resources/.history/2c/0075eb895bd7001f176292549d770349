header {
  font-family:"Hahmlet", serif;
  position: fixed;
  top:0;
  left: 0;
  color: #000000;
  height: 100px;
  width: 100%;
  padding: 20px 0;
  text-align: center;
}

header nav ul {
  font-family:"Hahmlet", serif;
  list-style-type: none;
  margin: 0;
  padding: 0;
}

header nav ul li {
  font-family:"Hahmlet", serif;
  display: inline;
  margin-right: 20px;
}

header nav ul li a {
  font-family:"Hahmlet", serif;
  color: #000000;
  text-decoration: none;
}

/* 전체 페이지 높이를 설정 */
body {
  margin: 0;
  padding: 0;
  height: 100vh; 
}

/* 메인 컨테이너 설정 */
.main {
  display: flex;
  flex-direction: column;
}

/* 헤더 설정 */
header {
  font-family:"Hahmlet", serif;
  position: fixed;
  top: 0;
  left: 0;
  color: #000000;
  height: 100px;
  width: 100%;
  padding: 20px 0;
  text-align: center;
  z-index: 1000;
}

/* 컨테이너의 위치 조정 */
.container {
   flex: 1;
  display: flex;
  justify-content: center; /* 수평 중앙 정렬 */
  align-items: center; /* 수직 중앙 정렬 */
  padding: 20px; /* 내용이 헤더에 겹치지 않도록 여백 조정 */
  box-sizing: border-box;
  max-width: 600px; /* 폼 최대 너비 설정 */
  width: 100%; 
  margin: 0 auto; /* 중앙 정렬을 위한 자동 마진 */
  border-radius: 10px; /* 모서리 둥글게 */
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* 그림자 효과 */
  background-color: #ffffff; /* 배경색 */
  flex-direction: column;
  margin-top: 220px;
  margin-bottom: 100px; /* 푸터와의 간격 추가 */
  background-color: #e9e9e9;
}

/* 푸터 스타일 */
#footers {
  background-color: #c4c4c4;
  color: #000;
  text-align: center;
  padding: 20px 0;
  font-size: 14px;
  height: 20%;
  width: 100%;
  position: fixed; /* 화면 하단 고정 */
  bottom: 0;
  left: 0;
}

h1 {
  font-family:"Hahmlet", serif;
  font-size: 24px;
  margin-bottom: 20px;
  color: #333;
}

.tabs {
  font-family:"Hahmlet", serif;
  display: flex;
  justify-content: space-around;
  margin-bottom: 20px;
}

.tab {
  font-family:"Hahmlet", serif;
  background: none;
  border: none;
  font-size: 16px;
  padding: 10px;
  cursor: pointer;
  color: #666;
  border-bottom: 2px solid transparent;
}

.tab.active {
  font-family:"Hahmlet", serif;
  color: #333;
  border-bottom: 2px solid #333;
}

.form {
  font-family:"Hahmlet", serif;
  text-align: left;
  margin-top: 40px;
  background-color: e9e9e9;
  
}

.hidden {
  font-family:"Hahmlet", serif;
  display: none;
}
#message {
  display: none; /* 초기 상태 숨김 */
  min-height: 15px; /* 최소 높이 설정 */
  margin-top: 10px;
  margin-bottom: 35px;
  transition: all 0.3s ease; /* 애니메이션 효과 추가 */
  font-family: "Hahmlet", serif;
  font-size: 15px;
  text-align: center;
}
#email-pw,
#email-code-pw{
	margin-left:23px;
}
#message.success {
  display: block; /* 성공 시 표시 */
  color: green;
}

#message.error {
  display: block; /* 오류 시 표시 */
  color: red;
}


/* 공통 스타일: 라벨, 입력 필드, 버튼 정렬 */
.input-group {
  font-family: "Hahmlet", serif;
  display: flex;
  align-items: center; /* 세로 정렬 */
  justify-content: space-between; /* 가로로 배치 */
  gap: 10px; /* 요소 간 간격 */
  margin-bottom: 40px; /* 그룹 간 간격 */
  width: 100%; /* 모든 그룹 동일한 너비 */
  
}

/* 라벨 스타일 */
.input-group label {
  font-family: "Hahmlet", serif;
  width: 100px; /* 라벨 고정 너비 */
  font-size: 14px;
  color: #333;
  white-space: nowrap; /* 텍스트 줄 바꿈 방지 */
}

/* 입력 필드 스타일 */
.input-group input {
  font-family: "Hahmlet", serif;
  flex: 1; /* 남은 공간 차지 */
  padding: 5px 0; /* 위아래 간격 */
  border: none; /* 테두리 제거 */
  border-bottom: 2px solid #ddd; /* 밑줄 스타일 */
  font-size: 14px;
  outline: none; /* 포커스 시 기본 테두리 제거 */
  background-color: transparent; /* 배경색 투명 처리 */
}

/* 입력 필드 포커스 스타일 */
.input-group input:focus {
  border-bottom: 1px solid #333; /* 포커스 시 밑줄 강조 */
}

/* 버튼 스타일 */
.input-group .send-code-btn {
  font-family: "Hahmlet", serif;
  padding: 8px 15px;
  background-color: #333;
  color: #fff;
  border: none;
  border-radius: 5px;
  font-size: 14px;
  cursor: pointer;
  white-space: nowrap; /* 줄 바꿈 방지 */
}

/* 버튼 호버 스타일 */
.input-group .send-code-btn:hover {
  background-color: #555;
}




.action-btn{
	font-family:"Hahmlet", serif;
  width: 30%;
  padding: 5px;
  background-color: #333;
  color: #fff;
  border: none;
  border-radius: 5px;
  font-size: 14px;
  cursor: pointer;
}
.action-btn:hover {
  font-family:"Hahmlet", serif;
  background-color: #555;
}
.submit-btn {
  font-family:"Hahmlet", serif;
  width: 100%;
  padding: 10px;
  background-color: #333;
  color: #fff;
  border: none;
  border-radius: 5px;
  font-size: 16px;
  cursor: pointer;
  margin-bottom:15px;
}

.submit-btn:hover {
  font-family:"Hahmlet", serif;
  background-color: #555;
}
 .modal {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%); 
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 1000;
    }

    .modal.hidden {
      display: none;
    }

    .modal-content {
      background: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      width: 400px;
      text-align: center;
    }

    .close-btn {
      margin-top: 20px;
      background-color: #333;
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .close-btn:hover {
      background-color: #555;
    }
 /* 모달 내 입력 그룹 스타일 */
.modal .input-group {
  display: flex;
  flex-direction: column; /* 수직 방향 정렬 */
  align-items: flex-start; /* 왼쪽 정렬 */
  margin-bottom: 15px; /* 그룹 간 간격 */
  width: 100%; /* 전체 너비 */
  position: relative; /* 에러 메시지 위치 조정을 위해 상대 위치 지정 */
}
.input-flied{
	margin-top:-30px;
}
/* 라벨과 입력 필드를 한 줄로 배치 */
.modal .input-group label,
.modal .input-group input {
  display: inline-block;
  width: 100%; /* 라벨과 입력 필드 너비를 부모에 맞춤 */
}

.modal .input-group label {
  font-size: 14px; /* 라벨 글자 크기 */
  margin-bottom: 5px; /* 라벨과 입력 필드 간격 */
  color: #333; /* 라벨 색상 */
}

/* 모달 내 입력 그룹 스타일 */
.modal .input-group {
  display: flex;
  flex-direction: row; /* 라벨과 입력 필드를 한 줄로 배치 */
  align-items: center; /* 세로 가운데 정렬 */
  justify-content: space-between; /* 라벨과 입력 필드 간격 유지 */
  margin-bottom: 30px; /* 그룹 간 간격 */
  width: 100%; /* 전체 너비 */
  position: relative; /* 에러 메시지 배치를 위한 상대 위치 */
  margin-top: 40px;
}

/* 라벨 스타일 */
.modal .input-group label {
  width: 30%; /* 라벨 너비 */
  font-size: 14px; /* 라벨 글자 크기 */
  color: #333; /* 라벨 색상 */
  white-space: nowrap; /* 줄 바꿈 방지 */
  margin-right: 10px; /* 입력 필드와 간격 */
}

/* 입력 필드 스타일 */
.modal .input-group input {
  width: 70%; /* 입력 필드 너비 */
  border: none; /* 기본 테두리 제거 */
  border-bottom: 2px solid #ccc; /* 밑줄만 표시 */
  
  padding: 8px 0; /* 내부 여백 조정 */
  font-size: 14px; /* 텍스트 크기 */
  outline: none; /* 포커스 시 기본 아웃라인 제거 */
  transition: border-color 0.3s ease; /* 부드러운 전환 효과 */
}

/* 입력 필드 포커스 시 스타일 */
.modal .input-group input:focus {
  border-bottom: 2px solid #007bff; /* 포커스 시 밑줄 색상 변경 */
}

/* 에러 메시지 스타일 */
.modal .error-message {
  color: red; /* 에러 메시지 색상 */
  font-size: 12px; /* 에러 메시지 크기 */
  margin-top: 5px; /* 입력 필드와의 간격 */
  display: none; /* 기본적으로 숨김 */
  position: absolute; /* 입력 필드 아래 배치 */
  left: 30%; /* 라벨 끝에 맞춤 */
  top: calc(100% + 5px); /* 입력 필드 아래로 간격 추가 */
  width: 70%; /* 입력 필드 너비에 맞춤 */
  line-height: 1.4; /* 줄 간격 */
}
#password-feedback{
	margin-left:7px;
	margin-right:5px;
}
#confirm-feedback{
	text-align:left;
	margin-left:20px;
}